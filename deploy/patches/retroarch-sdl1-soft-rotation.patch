diff --git a/gfx/drivers/sdl_gfx.c b/gfx/drivers/sdl_gfx.c
index 9178a4c..43b3532 100644
--- a/gfx/drivers/sdl_gfx.c
+++ b/gfx/drivers/sdl_gfx.c
@@ -61,11 +61,34 @@ typedef struct sdl_video
    uint8_t font_g;
    uint8_t font_b;
    bool quitting;
+   unsigned rotation;
+   void *rotate_buf;
+   size_t rotate_buf_size;
 } sdl_video_t;
 
 static void sdl_gfx_free(void *data)
 {
    sdl_video_t *vid = (sdl_video_t*)data;
+   unsigned rot = 0;
+   const void *src_frame = NULL;
+   unsigned src_w = 0;
+   unsigned src_h = 0;
+   unsigned src_pitch = 0;
+   unsigned old_w = 0;
+   unsigned out_w = 0;
+   enum scaler_pix_fmt fmt;
+   int bpp;
+   unsigned dst_pitch = 0;
+   const uint8_t *src = NULL;
+   uint8_t *dst = NULL;
+   unsigned y = 0;
+   unsigned x = 0;
+   size_t so = 0;
+   size_t dof = 0;
+   unsigned dx = 0;
+   unsigned dy = 0;
+   size_t need = 0;
+   SDL_Surface *srf = NULL;
    if (!vid)
       return;
 
@@ -78,6 +101,13 @@ static void sdl_gfx_free(void *data)
    scaler_ctx_gen_reset(&vid->scaler);
    scaler_ctx_gen_reset(&vid->menu.scaler);
 
+   if (vid->rotate_buf)
+   {
+      SDL_FreeSurface((SDL_Surface*)vid->rotate_buf);
+      vid->rotate_buf = NULL;
+      vid->rotate_buf_size = 0;
+   }
+
    free(vid);
 }
 
@@ -294,6 +324,9 @@ static void *sdl_gfx_init(const video_info_t *video,
    if (video->fullscreen)
       SDL_ShowCursor(SDL_DISABLE);
 
+   vid->rotation = 0;
+   vid->rotate_buf = NULL;
+   vid->rotate_buf_size = 0;
    sdl_gfx_set_handles();
 
    if (input && input_data)
@@ -358,6 +391,13 @@ static void sdl_gfx_check_window(sdl_video_t *vid)
    }
 }
 
+static void sdl_gfx_set_rotation(void *data, unsigned rotation)
+{
+   sdl_video_t *vid = (sdl_video_t*)data;
+   if (vid)
+      vid->rotation = rotation % 4;
+}
+
 static bool sdl_gfx_frame(void *data, const void *frame, unsigned width,
       unsigned height, uint64_t frame_count,
       unsigned pitch, const char *msg, video_frame_info_t *video_info)
@@ -368,6 +408,102 @@ static bool sdl_gfx_frame(void *data, const void *frame, unsigned width,
    bool menu_is_alive = (video_info->menu_st_flags & MENU_ST_FLAG_ALIVE) ? true : false;
 #endif
 
+   rot = vid->rotation;
+   src_frame = frame;
+   src_w = width;
+   src_h = height;
+   src_pitch = pitch;
+   old_w = width;
+   fmt = vid->scaler.in_fmt;
+   bpp = (fmt == SCALER_FMT_ARGB8888) ? 4 : 2;
+
+   /* Software rotation: SDL1 video driver has no native SET_ROTATION support
+    * (upstream issue #7447). Honor core rotation requests with a CPU transpose,
+    * mirroring the stock driver behavior. */
+   if (rot == 1 || rot == 3)
+   {
+      out_w = src_h;
+      need = (size_t)out_w * old_w * bpp;
+      if (vid->rotate_buf_size < need)
+      {
+         if (vid->rotate_buf)
+            SDL_FreeSurface((SDL_Surface*)vid->rotate_buf);
+         srf = SDL_CreateRGBSurfaceWithFormat(0, out_w, old_w, bpp * 8,
+               (fmt == SCALER_FMT_ARGB8888) ? SDL_PIXELFORMAT_ARGB8888
+                                             : SDL_PIXELFORMAT_RGB565);
+         vid->rotate_buf = srf;
+         vid->rotate_buf_size = need;
+      }
+      if (vid->rotate_buf)
+      {
+         srf = (SDL_Surface*)vid->rotate_buf;
+         src = (const uint8_t*)src_frame;
+         dst = (uint8_t*)srf->pixels;
+         dst_pitch = srf->pitch;
+         for (y = 0; y < src_h; y++)
+         {
+            for (x = 0; x < src_w; x++)
+            {
+               so = (size_t)y * src_pitch + (size_t)x * bpp;
+               if (rot == 1)
+               {
+                  dx = src_h - 1 - y;
+                  dy = x;
+               }
+               else
+               {
+                  dx = y;
+                  dy = src_w - 1 - x;
+               }
+               dof = (size_t)dy * dst_pitch + (size_t)dx * bpp;
+               if (bpp == 4)
+                  *(uint32_t*)(dst + dof) = *(const uint32_t*)(src + so);
+               else
+                  *(uint16_t*)(dst + dof) = *(const uint16_t*)(src + so);
+            }
+         }
+         src_frame = dst;
+         src_w = out_w;
+         src_h = old_w;
+         src_pitch = dst_pitch;
+      }
+   }
+   else if (rot == 2)
+   {
+      need = (size_t)src_w * src_h * bpp;
+      if (vid->rotate_buf_size < need)
+      {
+         if (vid->rotate_buf)
+            SDL_FreeSurface((SDL_Surface*)vid->rotate_buf);
+         srf = SDL_CreateRGBSurfaceWithFormat(0, src_w, src_h, bpp * 8,
+               (fmt == SCALER_FMT_ARGB8888) ? SDL_PIXELFORMAT_ARGB8888
+                                             : SDL_PIXELFORMAT_RGB565);
+         vid->rotate_buf = srf;
+         vid->rotate_buf_size = need;
+      }
+      if (vid->rotate_buf)
+      {
+         srf = (SDL_Surface*)vid->rotate_buf;
+         src = (const uint8_t*)src_frame;
+         dst = (uint8_t*)srf->pixels;
+         dst_pitch = srf->pitch;
+         for (y = 0; y < src_h; y++)
+         {
+            for (x = 0; x < src_w; x++)
+            {
+               so = (size_t)y * src_pitch + (size_t)x * bpp;
+               dof = (size_t)(src_h - 1 - y) * dst_pitch + (size_t)(src_w - 1 - x) * bpp;
+               if (bpp == 4)
+                  *(uint32_t*)(dst + dof) = *(const uint32_t*)(src + so);
+               else
+                  *(uint16_t*)(dst + dof) = *(const uint16_t*)(src + so);
+            }
+         }
+         src_frame = dst;
+         src_pitch = dst_pitch;
+      }
+   }
+
    if (!frame)
       return true;
 
@@ -381,14 +517,14 @@ static bool sdl_gfx_frame(void *data, const void *frame, unsigned width,
    video_frame_scale(
          &vid->scaler,
          vid->screen->pixels,
-         frame,
+         src_frame,
          vid->scaler.in_fmt,
          vid->screen->w,
          vid->screen->h,
          vid->screen->pitch,
-         width,
-         height,
-         pitch);
+         src_w,
+         src_h,
+         src_pitch);
 
 #ifdef HAVE_MENU
    menu_driver_frame(menu_is_alive, video_info);
@@ -571,7 +707,7 @@ video_driver_t video_sdl = {
    sdl_gfx_free,
    "sdl",
    NULL, /* set_viewport */
-   NULL, /* set_rotation */
+   sdl_gfx_set_rotation,
    sdl_gfx_viewport_info,
    NULL, /* read_viewport  */
    NULL, /* read_frame_raw */
