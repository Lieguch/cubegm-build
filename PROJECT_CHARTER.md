# CubeGM 开源重建 · 立项文件（Project Charter）

> 版本：v1.1 · 立项日期：2026-08-17 · 最近更新：2026-08-18（增 §零 19 条铁律级需求 + 版本语义更正：取消 vFinal 拆分，v1.0＝19 条完成＋真机验证）
> 维护：主理人齐活林（Qi）·交付总监 · 监督：用户
> 本文件定义 **"做什么 / 不做什么 / 为什么"**，是所有 Agent 决策的最高依据。
> **§零 铁律级需求** 凌驾本文件所有其他章节；item 0 与 item 14 由用户锁定，原文不可压缩/猜测/精简。

---

## §零 · 用户铁律级需求（19 条 · 2026-08-18 锁定基线）

> **本节为最高优先级。** 用户原文除用户主动修改外不可压缩、猜测、精简。
> **item 0 与 item 14 由用户明确锁定**（2026-08-18 用户原话："特别是第0条和第14条"），任何 Agent 不得修改。
> 任何修订须用户主动发起；本 Agent 不自行调整。

0. **【LOCKED】不变最终需求**：模仿 TreeFrogUI / picoarch / FrogUI 这套开源代码，构建一个功能对等甚至超越原厂 CubeGM 固件的系统；要用这套系统在原硬件上打游戏！我要你可以先了解后开始修复和构建工作，特别是这套系统的官网和知名模拟器RetroArch的官方说明文档，以及对应模拟器核心的官方文档。移植（57 核全表 + LDFLAGS_S + 子模块）

2. 需要你杜绝猜测、碰运气、推理、穷举试错等等无把握发散的处理工作方式；

3. 所有修复解决方法必须联网后搜集对应项目官网的文档、官方论坛、知名论坛的方法后分析有效解决手段，我要解决式的输出结果；

4. 极致压缩无关聊天内容，提高解决问题效率。

5. 压缩上下文时要求保留聊天历史上下文中关键要求；

6. 在不牺牲效率前提下节省token；因为按Token收费；

7. 在不牺牲效率前提下减少调用AI大模型的次数，需要在不触及AI大模型上限前提下进行，防止触发每分钟请求数量过多的事件；

8. 我需要这套系统支持官方全部核心，我要把硬件性能榨干。

9. Github的构建时间每个月只有2000分钟，请极致地节约使用，要求每次使用都是谨慎且最大利用率。

10. 在你准备开始前，请创建立项文件和交付要求，方便和其他AGent随时接手工作，你也需要创建Agent之间的交接流程，确保每个Agent接手都知道从哪里开始，结束后要同样创建交接文件。总体是让所有Agent都知道立项初始需求和最终交接要求，并且随时可以接受工作，也能随时让其他Agent接手。

11. 按照软件工程的流程，创建版本号、创建里程碑方便组合和回滚、创建验收清单，经行需求分析、实施方案确定、实施方案验收、提交代码等专业流程确保开发过程安全高质量。

12. 自动静默完成除铁律外所有检查、复盘、定位、修正、测试、上传、构建的行为，我默认允许；自动删除已完成、已取消、已失效的监控对话。

13. 我的RK3036G设备没有屏幕，只有HDMI输出RK3036G datasheet V1.1.pdf，附件是datasheet。文档中提及它支持1080P输出。所以不清楚是否软件系统最高支持1280*720？还是可以突破达到1080P。

14. **【LOCKED】11.png 目录结构 / 1.0 交付定义**：在你工作目录下的"11.png"是原系统工作目录截图，你的交付1.0版本首先按照这个目录截图交付，从000到008文件夹内的9个"Dat"文件沿用原设备的，不用额外构建记录,但需要你回忆从"000.dat"到"008.dat"中解析出的内容，方便对应游戏模拟器核心的指向和正常工作（对应从000.dat到008.dat共9个文件的情况从上下文调取）；对于9个文件夹外的模拟器核心工作目录指向"Roms"。

15. 启动劫持绝不改写 root.dat/校验分区，否则设备报「sdcard is damaged」；

16. 每次构建强制 verify_target_abi.sh，杜绝 glibc 错配上线，

17. 所有模拟器涉及策略游戏存储功能的，都要实现；

18. 需要支持所有手柄，至少可以添加手柄自定义映射。

### 0.1 锁定规则与执行纪律

- **item 0（不变最终需求 · LOCKED）**：原文必须原样保留，作为**19 条锁定基线的组成部分**与终极验收标准——57 核全表 + LDFLAGS_S + 子模块**必须在 1.0 之前完成**。本 charter 与所有交付物（HANDOFF / DELIVERY_REQUIREMENTS / VERSION / release notes）均不得压缩/猜测/精简其任何字句。**item 0 不是 1.0 之后的额外阶段，而是 1.0 必须包含的交付内容。**
- **item 14（11.png · 1.0 交付 · LOCKED）**：1.0 交付的 SD 卡目录结构**严格按 11.png 截图**。9 个 `.dat` 文件（000.dat–008.dat）沿用原设备，不重构建、不解析改写；ROM 路径以原设备分类为锚：9 个文件夹覆盖的核心用各自 `NNN/` 目录，9 个文件夹之外的核心指向 `Roms/`。
- **item 13（HDMI 分辨率）**：datasheet 已解析确认 RK3036G HDMI TX = HDMI 1.4a / HDCP 1.2，支持 480i→1080i/p，物理最大 1920×1080。1.0 暂用 **1280×720**（UI 资源天然匹配、最小风险）；1.x 可视情况开启 1920×1080 输出选项。
- **item 15-18（硬约束）**：与本 charter §二、§四既有内容互补；任一被违反视为回归。
  - 15 ↔ §二「启动安全」+ §四.10
  - 16 ↔ §四.12 + §五 ABI 门禁
  - 17 ↔ §四.2（策略游戏存档）
  - 18 ↔ §四.3（手柄 + 自定义映射）

### 0.2 19 条基线＝完整交付范围（关键更正 · 2026-08-18 用户纠正）

> **更正说明**：此前曾把 item 0（57 核全表）误判为"1.0 之后才推进的终极目标（vFinal）"。**这是错误的。** 用户 2026-08-18 明确：19 条需求（items 0–18）是**锁定基线**，全部必须完成；完成全部 19 条 + 用户真机全部验证通过后，版本号方可标记为 **1.0**。不存在独立于 1.0 之外的「vFinal」。

- **19 条基线＝完整交付范围**：含 item 0（57 核全表 + LDFLAGS_S + 子模块）、item 14（11.png 目录结构）、item 15–18（启动安全 / ABI 门禁 / 策略存档 / 全手柄）等，全部必须完成。
- **测试版 `v0.x`（含 `v0.0`）**：交付给用户真机测试用的中间版本；版本号保持 `0.0` 直到完整版（含 57 核）首次交付测试（用户 2026-08-18："版本号仍然为 0.0，因为从来没有提交过完整版给我测试"）。
- **`v1.0` 门槛**：**19 条基线全部完成** **且** **用户真机全部验证通过** 后，方可标记 `v1.0`。在此之前一律 `v0.x`（含 `0.0`）。
- **不存在「vFinal」**：57 核全表是基线的一部分，不是 1.0 之后的额外阶段；任何把 57 核挪到"1.0 之后"的表述均作废。

### 0.3 1.0 之前（测试版阶段）的任务推进纪律（用户 2026-08-18 红线）

> 用户原话："有关1.0交付后的任务列表，需要得到我反馈才能标记完成。"

- 1.0 之前的任务（含完成 19 条基线、移植 57 核、构建测试版）属交付基线本身，由 Agent 按 #12 静默自主推进；但**每次交付测试版给用户后，需用户反馈（真机验证结果）才能推进下一步或标记阶段完成**。
- 用户真机验证是 1.0 门槛的硬性闸门：**未收到用户"全部验证通过"的确认，不得标记 `v1.0`**。
- 本规则覆盖任何 Agent 自动化（#12 静默自主）——1.0 标记权归用户。

---

## 一、项目使命

**模仿 GitHub `tzubertowski/TreeFrogUI_picoarch` + `FrogUI` + `picoarch` + libretro 核心这套开源代码，构建功能对等甚至超越原厂 CubeGM 固件的开源替代系统，并在原硬件（RK3036G）上实际能打游戏。**

判定成功的唯一标准：**把 SD 卡插上 RK3036G 真机，按键进入菜单、选游戏、跑起来、出图、出声、存档、读档、全手柄可用**。

---

## 二、目标硬件

| 项 | 值 | 来源 |
|---|---|---|
| SoC | Rockchip RK3036G · 双核 Cortex-A7 @~1.0-1.2 GHz · ARMv7-A · NEON + VFPv4 · Mali-400MP | 用户确认 + 设备 ELF 实测 |
| 内存 | DDR3，约 256–512 MB | 设备规格 |
| 存储 | SD 卡（用户持有） | 实测 |
| **显示** | **无 LCD，仅 HDMI 输出** | 用户 2026-08-17 确认 |
| HDMI | datasheet 称支持 **1080P** 输出 | `R:\RK3036G datasheet V1.1.pdf`（用户引用） |
| 实际 UI 分辨率 | 1280×720 RGB565（设备 UI .raw 资源实测） | `docs/cubegm_input_and_ui.md` §三 |
| 系统 | 标准 buildroot Linux（DRM/KMS + ALSA + evdev） | `driver.so` 字符串实证 |
| 浮点 ABI | hard-float（armhf），glibc ≤ 2.17 | ELF `e_flags=0x5000400` + `tools/probe_glibc.py` |

### 2.1 分辨率决策（待解 · 重启后第一件事）

设备无屏、仅 HDMI；datasheet 称支持 1080P；但 UI 资源是 1280×720，stock root.dat 是 720×480。**软件系统最高支持 1280×720 还是能突破到 1080P** 必须读完 datasheet 的 HDMI/VOP/显示流水线章节才能定。

**暂行假设（待 datasheet 核实）**：
- 1.0 交付 1280×720（匹配既有 UI 资源，无需重做 720 张贴图；最小风险）。
- 1.x 增加 1920×1080 输出选项（让 HDMI 自动选最佳；UI 层做 720p→1080p 上采样）。
- 该决策需在 PROJECT_CHARTER 修订版 v1.1 中正式落定。**不可凭印象定**，必须 datasheet 实证。

---

## 三、原 9 条硬要求（用户既定，不可删减）

> 来源：用户 2026-08-13 起的对话原文 + prior context。**任何一条被删/改/弱化都需要用户明确授权。**

1. **完整构建整套系统**：要能编、能跑、能玩；不是 demo、不是 PoC。
2. **支持策略游戏存档**：state/save 完整、可用、可热切换。
3. **支持全部通用手柄**：USB HID 全覆盖；面板内置按键保留。
4. **杜绝猜测/碰运气/推理/穷举试错**：所有修复必须有真实日志或官方文档/源码证据。
5. **所有修复联网查对应项目官方真源后"解决式"输出**：每改一处都附"在哪个文件的哪一行改了什么、为什么、依据哪个官方源"；禁止"应该是 / 估计是 / 大概是"。
6. **极致压缩无关聊天内容**：不闲聊、不复述用户已知信息、不堆客套。
7. **压缩上下文保留关键要求**：总结/重启时关键要求必须可恢复。
8. **不牺牲效率前提下省 token / 减 AI 调用**：能并行的工具调用并行；能用工具读的别用 LLM 推。
9. **GitHub Actions 每月仅 2000 分钟，极致节约，单次最大利用率**：每次 push 触发的工作流必须**一次对**；失败/取消的 run 不计费（已实测），但**不要靠"取消重试"来碰运气**。

---

## 四、补充硬要求（2026-08-17 用户追加）

10. **1.0 交付按 `11.png` 目录结构**：SD 根 = `000/ 001/ 002/ 003/ 004/ 005/ 006/ 007/ 008/ cubegm/ Roms/ root.dat`。**9 个 `000.dat`–`008.dat` 文件沿用原设备，不重构建**；但**所有 Agent 接手都必须能立刻说清这 9 个 .dat 各对应哪个/哪些模拟器核心、ROM 怎么找**（见 §6.1）。9 个文件夹**之外**的模拟器核心，工作目录指向 `Roms/`。
11. **启动劫持绝不改写 `root.dat` / 校验分区**：否则设备立即报 `sdcard is damaged`，不可逆。
12. **每次构建强制 `tools/verify_target_abi.sh`**：杜绝 glibc 错配上线。CI 流程里是硬门禁，不通过即失败。
13. **项目治理（立项文件 / 交付要求 / Agent 交接流程 / 交接文件）**：所有 Agent 任何时候接手都能立刻知道"项目从哪来、到哪去、我在哪一步"，**随时能接手、随时能交班**。
14. **软件工程流程**：版本号、里程碑（可组合可回滚）、验收清单、需求分析、实施方案确定、实施方案验收、提交代码 —— 缺一不可；流程产物（PRD / 架构 / 任务列表 / 代码 / 测试报告）必须落盘可追溯。

---

## 五、用户红线（不可逾越 · 2026-08-16 正式界定）

> 此前本 charter 旧版把"修订铁律"误当红线；用户正式界定的红线只有以下三条；其余（GitHub API 推送修复、打测试版 tag、更新文档、跑本地门禁）均属"默认允许"，不请示。

1. **不得删除"创建者≠本 Agent"的文件/文件夹**：若任务非删不可，必须弹窗让用户选择，不得擅自删。
2. **涉及"不得不触犯铁律才有结果"的事**：必须让用户选择对该具体事项豁免铁律，不得自行豁免或自行触犯。
3. **不得删除"里程碑"性质的文件**：已被认同的成果（如 `core/*`、`v-*` tag、`HANDOFF.md`、`VERSION`、已验证修复）受保护，禁止删除。

> 执行纪律：碰到上述任一条 → **停下 + 弹窗/明确请示，绝不静默绕过**。

---

## 六、技术栈与已知约束（基于实证）

### 6.1 软件栈

| 层 | 选型 | 依据 |
|---|---|---|
| 工具链 | crosstool-NG gcc 13.2 / binutils 2.40 / glibc 2.17 / `arm-linux-gnueabihf-` | 设备 glibc ≤ 2.17 已锁 |
| 系统后端 | 标准 Linux（`libpicofe/linux/`：`in_evdev.c` / `sndout_alsa.c` / `fbdev.c`） | `driver.so` 字符串实证 + libpicofe 已提供 |
| 前端 | `TreeFrogUI_picoarch@r36sx`（`tzubertowski`） | 上游真源 |
| 启动器 | `FrogUI`（作 libretro core 运行，r36sx fork） | 上游真源 |
| 模拟器 | libretro 核心（mgba / snes9x / fceumm / picodrive / nestopia / vbam / gpsp / pcsx_rearmed / fba / mame2000 / ...） | 已建 sysroot + 已通过 STAGE2 编译 |
| 资源 | UI `.raw` 1280×720 RGB565（沿用原设备）；启动画面沿用 `root.dat` | 实测格式 |

### 6.2 平台后端真相（已联网核对上游源码确认）

- **FrogUI → picoarch 启动机制本身正确**：上游 `main.c:27` 定义 `LAUNCH_FILE = "/tmp/frogui_launch.txt"`；`quit()`(main.c:1107) 读该文件并 `execl()` core+rom；`RETRO_ENVIRONMENT_SHUTDOWN` 退出主循环。本工程 `patch/frogui_gs_bridge.patch`（写 launch 文件 + 调 SHUTDOWN）与上游一致，**保留勿改**。
- **真缺陷在 SF3000 平台后端**：`platform=sf3000` 激活 `plat_sf3000.c`（用 `cubevol` + `/tmp/joy_key` 共享内存、dlopen `driver.so`、`/dev/fb0` 手动 mmap）—— RK3036G 上全不存在。已切到 `platform=unix`（`plat_linux.c` → `plat_sdl.c` 纯 SDL 路径）。
- **`plat_sdl.c` 内 MStar `mi_sys.h` 块**：上游 `#ifndef PLATFORM_SF3000` 块假设有 MStar SDK（mi_sys / mi_gfx），RK3036G（Rockchip）无此 SDK。修复：把该块守卫改为 `#if !defined(PLATFORM_SF3000) && defined(PLATFORM_MSTAR)`，让 RK3036G 走同文件内的纯 SDL fallback。**该修复已识别、补丁就绪、待推送**。
- **SCREEN_WIDTH/HEIGHT/BPP 冲突**：`plat.h` 硬编 320×240×16；`scale.h` 在非 SF3000 下编 1280×960×32。需同时守卫两者，使 `-DSCREEN_WIDTH=1280 -DSCREEN_HEIGHT=720 -DSCREEN_BPP=2` 生效。

### 6.3 待补事实（接手后第一周内必须完成，禁止猜测）

- **`000.dat`–`008.dat` 9 个目录内 .dat 文件的实际内容解析**：原分析（`cubegm_analysis` / `input_and_ui` / `root_dat_analysis` / `zip_contents`）**未解析**这 9 个文件。接手 Agent 必须直接读原设备的 9 个 .dat，输出"目录号 → 模拟器核心 → ROM 路径规则"对照表，写入 `docs/dat_000_008_mapping.md`。
- **datasheet 显示/HMI 章节完整阅读**：决定 1.0 渲染分辨率（1280×720 vs 1920×1080）。

---

## 七、非目标（Out of Scope）

明确**不做**的事，避免范围漂移：

- 不做 OTA 升级通道（`update/firmware.upk` 暂搁置）。
- 不做金手指/作弊系统（`cores/mame2000/{cheat,artwork,sta}` 素材补全暂搁置）。
- 不做多语言 UI 全开（仅保留中文 `gamelist=1`，其余 8 种语言延后）。
- 不做收藏夹/历史列表数据迁移（结构就位、数据空着）。
- 不动 `core/*` 已验证 tag、`HANDOFF.md`、`VERSION`、里程碑文件（红线 §五.3）。
- 不重写原设备 9 个 `.dat`（沿用原设备，红线 §四.10）。

---

## 八、治理与协作

- **本工程已选 Expert**：`SoftwareCompany`（主理人齐活林 · 交付总监）。
- **团队成员**：产品经理许清楚 / 架构师高见远 / 工程师寇豆码 / QA 严过关。
- **流程触发**：仅当用户给出**新功能需求**走标准 SOP；**Bug 修复**走 BugFix 快捷路径（工程师定位修复 + QA 回归）；**当前主任务 = 修 `mi_sys.h` 编译失败 + 推 1.0 完整构建**属 BugFix 路径。
- **流程产物**：`docs/prd/*.md` / `docs/arch/*.md` / `docs/tasks/*.md` / `docs/test/*.md` 全留痕。

---

## 九、修订记录

| 版本 | 日期 | 修订人 | 修订内容 |
|---|---|---|---|
| v0.1 | 2026-08-13 | 主理人 | 初稿（仅 9 条硬要求） |
| v0.9 | 2026-08-16 | 主理人 | 增 §五 用户红线（正式界定 3 条） |
| v1.0 | 2026-08-17 | 主理人 | 增 §四 5 条补充硬要求（1080P / 11.png 目录 / 9 .dat 沿用 / 启动不改 root.dat / verify_abi）；增 §二 2.1 分辨率待解；增 §六.3 待补事实；增 §八 治理与协作。 |
| **v1.1** | **2026-08-18** | **主理人** | **新增 §零 19 条铁律级需求（用户 2026-08-18 提交；item 0/14 锁定）；新增 §零.1 锁定规则、§零.2「19 条基线＝完整交付范围」更正（取消 vFinal 拆分）、§零.3 1.0 之前推进纪律。更正此前"57 核＝vFinal"的错误。** |

---

*本 charter 一经发布即对所有 Agent 生效；任何修订需主理人复核 + 在修订记录留痕。*
