name: rkgame-rebuild

on:
  push:
    paths:
      - "rkgame-rebuild/**"
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4
      - run: |
          echo "Building rkgame..."
          ls -la rkgame-rebuild/
          ls -la rkgame-rebuild/src/
          pwd
