# CubeGM 版本号（用户规定）
# 1.0 = 第一个用户能正式使用、全部功能正常的版本。
# 1.0 之前全为测试版；首个完整绿构建 = v0.1，之后 v0.2、v0.3...（每次实质进展递增）
# 整构建首绿且真机功能验收通过 -> 打 v1.0 tag + 维护 stable 分支。

当前：0.3.1-dev
状态：v0.1 已打 tag（首个全绿构建，run #145 HEAD 05ee252，前端+5核 ABI 全 PASS）；v0.2 已打 tag（run #153 HEAD 377ac862208a，含全量测试门禁：STAGE8 ABI + STAGE8.5 符号 + STAGE9.5 完整性，34 核全绿）；v0.3 进行中（MD 策略存档写权限加固 #162/#166，run #166 HEAD cb065b23）。
2026-08-18 修复+验证：STAGE9 打包 SDL/libpng12/zlib 运行时库进 cubegm/lib（run #150 全绿，lib/ 三个库 symlink 链完整，6 产物 ABI 全 PASS，release payload-150 已发布）。
2026-08-18 datasheet：RK3036G SoC Max output 1920x1080（HDMI 1.4a），1280x720 维持。
里程碑 tag：core/stage1-sysroot、core/stage2-perms-fixed、core/stage2-libs、v-sysroot-writable、v-platsf3000-syntax、v0.1、v-build-good、v0.2、stable。
2026-08-18 MD 策略存档：#162 注入 save_dir 可写性回退+诊断（cubegm/saves）；run #165 因 fprintf 字符串内 Python \n 误转真实换行编译失败（core.c:359 missing terminating "）；#166 转义修复（cb065b23），待 run #166 绿后打 v0.3。
2026-08-22 v0.3.1（payload-278 实机 10 项问题根治轮，run 待 CI）：
  - 输入：cubevol_bridge 方向轴"值==0 误判未插"修复（上/左失效根因）；evdev 绑定表 0x120-0x12b 对齐原厂 profile（A/B/X/Y/L/R/SELECT/START 错位修复）；游戏输入 OR 入 shm 直连（与 UI 同源双通道）；摇杆数字化取消 abs_to_digital 门控；frogui L/R 翻页改绑 CV_L/CV_R 肩键（原错绑左右方向键）。
  - 音频：picoarch 链接加 -rdynamic（dlsym plat_sound_finish 成功）；fork 前释放 ALSA PCM；子进程 snd_config_update_free_global 兜底（EBUSY 根治）。
  - 显示/性能：竖屏 90° 旋转预计算像素映射表（1941 等竖屏 25→60FPS）；DRM/fb0 fd 加 O_CLOEXEC + RMFB 清理（半白屏根因=子进程关闭父进程 DRM fd）；frogui 根目录只显目录（首菜单加速）。
  - 核心映射：002→snes9x2005_plus（CI 优化版，替代设备上未优化 core 的 1FPS）；005→nestopia（原厂 005 用 libemu_nestopia，fceumm 黑屏修复）。
  - 诊断：zhijack.sh 每次启动写 crash.log 头行（证明 debug 路径存活）；in_evdev 打印前 40 个按键码（下次实机验证绑定匹配）。
  - 构建：picoarch/frogui 改为单一全量补丁（picoarch_rk3036g_full.patch / frogui_rk3036g_full.patch），消除多补丁叠加失配（run 277 根因）。
