# CubeGM 版本号（用户规定）
# 1.0 = 第一个用户能正式使用、全部功能正常的版本。
# 1.0 之前全为测试版；首个完整绿构建 = v0.1，之后 v0.2、v0.3...（每次实质进展递增）
# 整构建首绿且真机功能验收通过 -> 打 v1.0 tag + 维护 stable 分支。

当前：0.1.0-dev
状态：STAGE1/STAGE2 已验证通过（run34 日志证据）；STAGE3-7 修复（STAGE7 per-core + 根目录 int 补丁 + zlib 加固）2026-08-17 已推送（main=82a7d83），待重新 dispatch 验证。
尚未打 v0.1 tag（等首个全绿构建）。
